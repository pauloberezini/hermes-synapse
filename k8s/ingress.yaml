apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hermes-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: hermes-backend
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hermes-frontend
            port:
              number: 80
